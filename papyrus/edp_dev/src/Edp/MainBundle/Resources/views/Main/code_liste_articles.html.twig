{% extends "EdpMainBundle::page_niv2.html.twig" %}

{% block n3 %}


    <h2 style="text-align: center">Liste des articles liés au "<em> {{code.titre}} </em>"</h2><br /><br />

     {#<br>{{user.username}}
    <br>
    {{abo.libelle}}
    <br>#}

    {% for article in articles %}

        <div class="row">
            <div class="col-sm-10 col-sm-offset-1">
                <div class="login-form">

                    <table class="table table-striped table-hover record_properties">
                        <thead>
                            <tr style="background:#F0F0E9">
                                <th>Titre</th>
                                <th>N° NOR</th>
                                <th>Date</th>
                                <th style="text-align:center">Action</th>
                            </tr>
                        </thead>
                        <tbody>
                            <tr>
                                <td>{{article.titre}}</td>
                                <td>{{article.nor}}</td>
                                <td>{{article.date|date('Y-m-d')}}</td>
                                <td>
                                    <ul>
                                        <li>
                                            <a href="{{ path("edp_article_consult",{'id':article.id}) }}"> Consulter </a>
                                        </li>
                                    </ul>
                                </td>
                            </tr>
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
                                        
{# ****************TROUVER UNE SOLUTION POUR RETOUR PAGE PRECEDENTE********************** !#}
                                        
       {# <div class='row'>    
            <div class='col-sm-3 col-sm-offset-5'>            
                <ul class="record_actions">
                    <li>
                        <a href="{{ path("edp_abo_liste_codes",{'id':code.id})}}">
                            Retour à la liste des codes
                        </a>
                    </li>
                </ul>
            </div>
        </div>#}

        {# <p>{{code.titre}}</p>
         <p>{{code.description}}</p>#}

    {% endfor %}

{% endblock n3 %}